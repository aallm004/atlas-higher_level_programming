#!/usr/bin/python3
"""funciton that returns True if obj is exactly an instance
    of the specified class; otherwise False"""


def is_same_class(obj, a):
    """
    funciton that returns True if obj is exactly an instance
    of the specified class; otherwise False
    """
    if type(obj) is a:
        return True
    else:
        return False
